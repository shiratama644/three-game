# three-game

Babylon.js + React + Vite で作成したモバイル対応 FPS プロトタイプです。

## 起動

```bash
npm install
npm run dev
```

## 操作

- PC: `WASD` / `矢印` で移動、`Space` でジャンプ、`Shift` でスライド、クリック後マウスで視点操作
- スマホ: 左スティックで移動、右側ドラッグで視点操作、`Jump` / `Slide` ボタンでアクション
